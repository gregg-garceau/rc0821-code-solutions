/* exported getFirstChar */
// Define a function getFirstChar with a parameter of a string
function getFirstChar(string) {
  // Return the value of the string at the first index of the string using bracket notation to access the character at that index.
  return string[0];
}
