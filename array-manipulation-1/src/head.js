/* exported head */
// Define a function with the name head that takes a parameter of array.
function head(array) {
  // Find the first value of the array by getting value of array at index zero.
  return array[0];
  // Returning the value of the array at the index of zero.
}
